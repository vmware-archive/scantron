---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang

inputs:
- name: scantron
- name: scantron-binary
- name: version

outputs:
- name: release-params

run:
  path: scantron/ci/scripts/release
